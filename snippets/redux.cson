{
  ".source.js":
    "redux: action function":
      "prefix": "raf"
      "body": """
        export function list( ${1} ) {
          return {
            type: ${2},
            ${3}
          }
        }
      """

    "redux: reducer function":
      "prefix": "rrf"
      "body": """
        function(state = ${1}, action) {
          switch (${2}) {
            case ${3}:
              return ${4}
            default:
              return state;
          }
        }
      """
}
